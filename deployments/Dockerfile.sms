FROM golang:1.24-alpine AS builder

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .


RUN go build -o /sms_worker ./cmd/sms_worker

FROM alpine:latest

COPY --from=builder /sms_worker /sms_worker

EXPOSE 3003
CMD ["/sms_worker"]
